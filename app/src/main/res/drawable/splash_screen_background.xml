<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/orange_primary" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/touccan_preto"
            android:scaleType="centerInside" /> <!-- Ajusta a imagem para que não ocupe a tela toda -->
    </item>
</layer-list>